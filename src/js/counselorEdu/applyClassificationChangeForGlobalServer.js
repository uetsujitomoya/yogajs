let applyClassificationChangeForGlobalServer = () => {
  getClassificationChange(csv)
  applyClassificationChangeForNextTime(csv)
}

let getClassificationChange = (csv) => {
    // 出力するやつを、サーバ上に出力保存（これさえやれば良い気がする）
}

let applyClassificationChangeForNextTime = (csv) => {
    // 読ませる（多分自動で読んでくれる）
}
